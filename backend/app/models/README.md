# Modelle

ORM- oder Persistenzmodelle fuer die Wissensbasis.

Die V1-Modelle sollen bereits Platz fuer spaetere `workspace_id`- und `owner_user_id`-Felder lassen, ohne Mehrbenutzerlogik zu implementieren.